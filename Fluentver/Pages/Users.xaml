<?xml version="1.0" encoding="utf-8"?>
<controls:InfoPage
    x:Class="Fluentver.Pages.Users"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fluentver.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Fluentver.Controls"
    NavigationCacheMode="Required"
    mc:Ignorable="d">
    <controls:InfoPage.ToolbarButtons>
        <controls:GlyphButton Glyph="&#xE8FA;" Text="Manage other users" ProcessName="ms-settings:otherusers" />
    </controls:InfoPage.ToolbarButtons>

    <Expander x:Name="userInfo" HorizontalContentAlignment="Left" HorizontalAlignment="Stretch" IsExpanded="True" Padding="0" Header="Your info">
        <StackPanel Orientation="Horizontal">
            <Grid Margin="12">
                <Ellipse Height="50" Width="50" PointerEntered="UserPhoto_PointerEntered">
                    <Ellipse.Fill>
                        <ImageBrush x:Name="userPhotoImage" ImageSource="ms-appx:///Assets/StoreLogo.png" />
                    </Ellipse.Fill>
                </Ellipse>
                <Grid x:Name="cameraHover" Visibility="Collapsed">
                    <Ellipse x:Name="cameraHoverColor" PointerPressed="UserPhoto_PointerPressed" PointerReleased="UserPhoto_PointerReleased" PointerExited="UserPhoto_PointerExited" Fill="Black" Opacity="0.5" Height="50" Width="50" />
                    <FontIcon Glyph="&#xE722;" PointerPressed="UserPhoto_PointerPressed" PointerReleased="UserPhoto_PointerReleased" PointerExited="UserPhoto_PointerExited" Foreground="White" />
                </Grid>
            </Grid>
            <StackPanel Spacing="4" Margin="0,12,12,12" VerticalAlignment="Center">
                <TextBlock x:Name="userDisplayName" Text="Display name" IsTextSelectionEnabled="True" TextWrapping="NoWrap" />
                <TextBlock x:Name="userAccountName" Text="Email/user name" Style="{ThemeResource SecondaryDisplayTextBlockStyle}" TextWrapping="NoWrap" />
            </StackPanel>
        </StackPanel>
    </Expander>
    <Expander HorizontalContentAlignment="Stretch" HorizontalAlignment="Stretch" IsExpanded="True" Padding="0" Header="Other users">
        <Grid>
            <ListView x:Name="usersList" SelectionMode="None" Margin="-4,0,0,0" MaxHeight="193" ItemsSource="{x:Bind users}" />
            <TextBlock x:Name="otherUsersLabel" Text="No other users found." HorizontalAlignment="Center" Visibility="Collapsed" />
        </Grid>
    </Expander>
</controls:InfoPage>
